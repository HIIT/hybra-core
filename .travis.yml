language: python

install:
  - pip install requests
  - pip install dateparser

script: python -m pytest

notifications:
  email:
    recipients:
      - juho.paakkonen@helsinki.fi

    on_success: change
    on_failure: always
